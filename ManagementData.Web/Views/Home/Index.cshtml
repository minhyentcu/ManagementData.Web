@using Microsoft.AspNet.Identity;
@{
    ViewData["Title"] = "Home";
    Layout = "~/Views/Shared/_Adminlte.cshtml";
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            List Data
        </h1>
        <ol class="breadcrumb">
            <li><a href="/"><i class="fa fa-dashboard"></i> Home</a></li>
            <li><a href="#">Index</a></li>
            <li class="active">Datas </li>
        </ol>
    </section>
    <!-- Main content -->
    <section class="content">
        <input type="hidden" id="userId" value="@User.Identity.GetUserId()" />
        <input type="hidden" id="idLast" value="0" />
        <div class="row data-content-user">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Data</h3>
                        <button type="button" class="btn btn-success" style="float:right" data-toggle="modal" data-target="#modal-default">
                            Create New
                        </button>
                        @*<button type="button" class="btn btn-primary btn-export" style="float:right;margin-right:1%" >
                               Export
                            </button>*@
                        <a href="/Data/Export/?userId=@User.Identity.GetUserId()" data-windowId="@Guid.NewGuid().ToString()" class="btn btn-primary btn-export" style="float:right;margin-right:1%">Export</a>
                        <button class="btn btn-primary btn-import" style="float:right;margin-right:1%">Import</button>
                        <form method="post" action="/Data/Import" style="display:none" enctype="multipart/form-data">
                            <input id="file-import" hidden type="file" name="file" accept=".xls,.xlsx,.txt" />
                            <input id="" hidden type="text" name="userId" value="@User.Identity.GetUserId()" />
                            <button type="submit" class="btn btn-primary btn-submit-import">Submit</button>
                        </form>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
                        <div class="col-12">
                            <table id="example2" class="table table-bordered table-hover" style="width:100%">
                                <thead>
                                    <tr>
                                        <th class="text-center">STT</th>
                                        <th class="text-center">Text</th>
                                        <th class="text-center">Create Date</th>
                                        <th class="text-center">
                                            <button class="btn btn-danger btn-sm btn-delete-all">Delete</button>
                                            <button class="btn btn-sm btn-select-all btn-info" data-slect="false"> Select All</button>

                                        </th>
                                    </tr>
                                </thead>
                                <tfoot>
                                    <tr>
                                        <th></th>
                                        <th colspan="3" style="text-align:right">Total:</th>
                                    </tr>
                                </tfoot>
                            </table>
                        </div>
                    </div>
                    <!-- /.box-body -->
                </div>
            </div>
            <!-- /.col -->
        </div>

        <!-- /.row -->

        <div class="modal fade" id="modal-default">
            <div class="modal-dialog ">
                <div class="modal-content " style="width:auto !important">
                    <form method="post" action="/Data/InsertData" enctype="multipart/form-data">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                            <h4 class="modal-title">Inser Data</h4>
                        </div>
                        <div class="modal-body">
                            <textarea class="form-control" rows="10" name="Text" style="width:100%"></textarea>
                            <input type="hidden" name="userId" value="@User.Identity.GetUserId()" />
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Close</button>
                            <button type="submit" class="btn btn-primary">Save changes</button>
                        </div>
                    </form>
                </div>
                <!-- /.modal-content -->
            </div>
            <!-- /.modal-dialog -->
        </div>
        <div class="modal fade" id="modal-edit">
            <div class="modal-dialog">
                <div class="modal-content">
                    <form method="post" action="/Data/UpdateData">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                            <h4 class="modal-title">Edit Data</h4>
                        </div>
                        <div class="modal-body">
                            <textarea class="form-control data-text-edit" rows="10" name="Text"></textarea>
                            <input type="hidden" name="id" id="data-id-edit" value="" />
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Close</button>
                            <button type="submit" class="btn btn-primary">Save changes</button>
                        </div>
                    </form>
                </div>
                <!-- /.modal-content -->
            </div>
            <!-- /.modal-dialog -->
        </div>
    </section>
    <!-- /.content -->


</div>
<!-- /.content-wrapper -->


@section Styles{
    <!-- Bootstrap 3.3.7 -->
    <link rel="stylesheet" href="~/Content/adminlte/components/bootstrap/dist/css/bootstrap.min.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="~/Content/adminlte/components/font-awesome/css/font-awesome.min.css">
    <!-- Ionicons -->
    <link rel="stylesheet" href="~/Content/adminlte/components/Ionicons/css/ionicons.min.css">
    <!-- DataTables -->
    <link rel="stylesheet" href="~/Content/adminlte/components/datatables.net-bs/css/dataTables.bootstrap.min.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="~/Content/adminlte/dist/css/AdminLTE.min.css">
    <!-- AdminLTE Skins. Choose a skin from the css/skins
         folder instead of downloading all of them to reduce the load. -->
    <link rel="stylesheet" href="~/Content/adminlte/dist/css/skins/_all-skins.min.css">
    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    <!-- Google Font -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,600,700,300italic,400italic,600italic">
    <style>
        #example3_filter {
            display: none;
        }

        .cb-delete {
            margin-left: 4% !important;
        }

        input.cb-delete {
            width: 20px;
            height: 20px;
        }
        #example2_info{
            display:none;
        }
        .text-break{
            word-break:break-all;
        }
    </style>
}


@section Scripts{
    <!-- jQuery 3 -->
    <script src="~/Content/adminlte/components/jquery/dist/jquery.min.js"></script>
    <!-- Bootstrap 3.3.7 -->
    <script src="~/Content/adminlte/components/bootstrap/dist/js/bootstrap.min.js"></script>
    <!-- DataTables -->
    <script src="~/Content/adminlte/components/datatables.net/js/jquery.dataTables.min.js"></script>
    <script src="~/Content/adminlte/components/datatables.net-bs/js/dataTables.bootstrap.min.js"></script>
    <!-- Slimscroll -->
    <script src="~/Content/adminlte/components/jquery-slimscroll/jquery.slimscroll.min.js"></script>
    <!-- FastClick -->
    <script src="~/Content/adminlte/components/fastclick/lib/fastclick.js"></script>
    <!-- AdminLTE App -->
    <script src="~/Content/adminlte/dist/js/adminlte.min.js"></script>
    <!-- AdminLTE for demo purposes -->
    <script src="~/Content/adminlte/dist/js/demo.js"></script>

    <script src="~/Scripts/sweetalert.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery-cookie/1.4.1/jquery.cookie.min.js" defer></script> 
    <!-- page script -->
    <script>
        $(document).ready(function () {
            var idUser = $('#userId').val();
            var idLast = $('#idLast').val();
            var model = {
                userId: idUser,
                idLast: 0,
            };
            var stringData;
            var url = "/Data/GetData/?userId=" + idUser + "&idLast=" + idLast;
            $('#example2').DataTable({
                "ajax": {
                    "url": "/Data/GetData",
                    "type": "GET",
                    "data": function (d) {
                        d.userId = $('#userId').val();
                        d.idLast = $('#idLast').val();
                    },
                    "dataType": "json",
                    "dataSrc": function (json) {
                        //var api = this.api();
                        var idUser = $('#userId').val();
                        var windowId = $('btn-export').attr('data-windowId');
                        var urlExport = "/Data/Export/?userId=" + idUser + "&page=" + json.page + "&size=" + json.size + "&orderSort=" + json.orderSort + "&windowId=" + windowId;
                        $('#example2_info').html("Total records: " + json.countData);
                        stringData = $('#example2_info').html();
                        $('.btn-export').attr('href', urlExport);
                        return json.dataObject;
                    }
                },
                drawCallback: function (response) {
                    console.log(response);
                },
                footerCallback: function (row, data, start, end, display,json) {
                    //console.log(data);
                    var api = this.api();
                    $(api.column(3).footer()).html(stringData);
                },
                "columns": [
                    { "data": "Stt", "name": "Stt", "width": "5%" },
                    { "data": "Text", "name": "Text", "width": "70%", "className": "text-break"  },
                    { "data": "TimeString", "name": "TimeString", "width": "10%"},
                    {
                        "data": "Id",
                        "render": function (data, row, type) {
                            return `
                                         <div class="text-center">
                                        <a  class="btn btn-info text-white btn-edit" data-id="${data}"data-toggle="modal" data-target="#modal-edit"  style="cursor:pointer">
                                           Edit  &nbsp;
                                        </a>
                                        <a  class="btn btn-danger text-white btn-delete" data-id="${data}" style="cursor:pointer">
                                           Delete <i class="fas fa-trash-alt"></i> &nbsp;
                                        </a>
                                        <input type="checkbox" class="cb-delete" name="item-data" data-id="${data}" value="${data}" name="cb-delete">
                                        </div>
                                         `
                        },
                        "width": "15%"
                    }
                ],
                "serverSide": "true",
                "searching": false,
                "ordering": false,
                "recordsTotal":100,
                
            });
            $("div.toolbar").html('<b>Custom tool bar! Text/images etc.</b>');
            $('#example2_info').html('');
            var table = $('#example2').DataTable();
            var data;
            $('#example2_info').html('');
            $('#example2 tbody').on('click', 'tr', function () {
                data = table.row(this).data();
            });
            $('body').on('click', '.btn-edit', function (e) {
                e.preventDefault();
                var id = $(this).attr('data-id');
                $('#data-id-edit').val(id);
                $('.data-text-edit').html(data.Text);
            });

            $('body').on('click', '.btn-delete', function (e) {
                e.preventDefault();
                var id = $(this).attr('data-id');
                swal({
                    title: 'Are you sure?',
                    text: 'Once deleted, you will not be able to recover this data!',
                    icon: 'warning',
                    buttons: true,
                    dangerMode: true,
                })
                    .then((willDelete) => {
                        if (willDelete) {

                            $.ajax({
                                type: "POST",
                                url: "/Data/DeleteData",
                                data: "id=" + id,
                                dataType: "json",
                                success: function (response) {
                                }
                            });
                            location.reload();
                        } else {
                            //swal('Your imaginary file is safe!');
                        }
                    });
            });

            $('.btn-import').click(function (e) {
                e.preventDefault();
                $('#file-import').trigger('click');
            });

            $('#file-import').on('change', function () {
                $('.btn-submit-import').trigger('click');
            });

            $('.btn-select-all').click(function (e) {
                e.preventDefault();
                var check = $(this).attr('data-slect');
                checkboxes = $(':checkbox');
                if (check == "false") {
                    $(this).attr('data-slect', 'true');
                    checkboxes.prop('checked', true);
                }
                else {
                    $(this).attr('data-slect', 'false');
                    checkboxes.prop('checked', false);
                }
            });
            // btn delete all item

            $('body').on('click', '.btn-delete-all', function (e) {
                e.preventDefault();
                var dataItems = [];
                $.each($("input[name='item-data']:checked"), function () {
                    dataItems.push($(this).val());
                });
                var modelPost = {
                    dataItems: dataItems,
                    admin: ''
                };
                swal({
                    title: 'Are you sure?',
                    text: 'Once deleted, you will not be able to recover this data!',
                    icon: 'warning',
                    buttons: true,
                    dangerMode: true,
                })
                    .then((willDelete) => {
                        if (willDelete) {
                            $.ajax({
                                type: "POST",
                                url: "/Data/DeleteAllData",
                                data: modelPost,
                                dataType: "json",
                                success: function (response) {
                                }
                            });
                            location.reload();
                        } else {
                            //swal('Your imaginary file is safe!');
                        }
                    });
            });
            // btn delete all item

            $('.btn-export').click(function (e) {
                //e.preventDefault();
                checkCookie();
            });
            //var table = $('#example2').DataTable();
            //setInterval(function () {
            //    table.ajax.reload();
            //    console.log("reload");
            //}, 3000);
        });
        function checkCookie() {
            var cookieVal = $.cookie($("input[name='windowid']").val());
            if (cookieVal == null || cookieVal === 'undefined') {
                setTimeout("checkCookie();", 1000);
            }
            else {
                var table = $('#example2').DataTable();
                setInterval(function () {
                    table.ajax.reload();
                    console.log("reload");
                }, 3000);
            }
        };
       
    </script>

}

